{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Manifest",
  "description": "One file's worth of schema — serves both root manifests and included modules; the resolver enforces which fields are legal in which role.",
  "type": "object",
  "required": [
    "version"
  ],
  "properties": {
    "commands": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/CommandDef"
      }
    },
    "description": {
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "includes": {
      "description": "Root manifests only. Local `./path` sources in Phase A.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/IncludeDef"
      }
    },
    "name": {
      "description": "Display name shown in the guided flow header; defaults to the directory name.",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "params": {
      "description": "Named, reusable param definitions (referenced via `use:`).",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/ParamDef"
      }
    },
    "registries": {
      "description": "Reserved for Phase C — rejected with a clear message if present.",
      "default": null
    },
    "steps": {
      "description": "Named, reusable script fragments (referenced via `use:` in run lists).",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/StepDef"
      }
    },
    "vars": {
      "description": "Modules only: variables the include site binds.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/VarDef"
      }
    },
    "version": {
      "description": "Checked via `VersionProbe` before full parsing; kept so the schema is complete and future versions can branch on it.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    }
  },
  "additionalProperties": false,
  "definitions": {
    "CommandDef": {
      "type": "object",
      "required": [
        "id",
        "run",
        "title"
      ],
      "properties": {
        "category": {
          "description": "Display grouping for the guided flow, the palette, and `--list`: an author-assigned label (\"Deploy\", \"Tests\"). Commands sharing a category are grouped together regardless of which file declared them — a module tagging its exports \"Deploy\" joins the local \"Deploy\" group. When unset, grouping falls back to the include origin (see `ResolvedCommand::group_label`), so a manifest with no categories at all degrades to today's flat list/menu.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "check": {
          "description": "Optional readiness probe: a shell command (no `{param}` placeholders — it runs before params exist) whose exit 0 means \"ready to run\". Surfaced by `pult doctor` and by UIs before the run button; never run implicitly before `run:` — preflight steps in the playbook remain the mitigation.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "description": "One or two sentences explaining what the command does, shown by `--help`, `--list --json`, and UIs — the title names the control, the description explains it.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "type": "string"
        },
        "interactive": {
          "description": "Declares that `run:` requires a controlling terminal at runtime (REPLs, TUIs, shells into containers). The contract: an *undeclared* command must be fully non-interactive once its params are filled — declare a param, don't `read` — which is what makes non-terminal surfaces safe. The plain CLI ignores it (stdio is inherited either way).",
          "default": false,
          "type": "boolean"
        },
        "params": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/ParamDef"
          }
        },
        "run": {
          "$ref": "#/definitions/RunSpec"
        },
        "title": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "FullOption": {
      "type": "object",
      "required": [
        "value"
      ],
      "properties": {
        "description": {
          "description": "Shown as `value — description` in the picker; display-only.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "value": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "FullStep": {
      "type": "object",
      "required": [
        "script"
      ],
      "properties": {
        "outputs": {
          "description": "Shell variable names this step promises to set.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "script": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "IncludeDef": {
      "type": "object",
      "required": [
        "source"
      ],
      "properties": {
        "prefix": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "sha256": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "source": {
          "type": "string"
        },
        "vars": {
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "InputDef": {
      "type": "object",
      "properties": {
        "default": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "secret": {
          "description": "Secret values are prompted without echo and masked wherever an interpolated command line is displayed (`running:` banner, `--print`, the ephemeral trust prompt). A `default:` is rejected for secrets — a default would be a credential committed to the manifest.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "OptionDef": {
      "description": "A pick option: a plain value, or a value with a display description.\n\nDeliberately *not* `#[serde(untagged)]` — see the manual `Deserialize` below for why. The `JsonSchema` derive can't read a `#[serde(untagged)]` attribute off a manually-`Deserialize`d type, so it gets schemars' own untagged marker (test-only) to keep the emitted schema \"string-or-mapping\", matching the `StepDef` idiom.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "$ref": "#/definitions/FullOption"
        }
      ]
    },
    "ParamDef": {
      "description": "A param is a picker, free input, or a reference to a named param; exactly one of the three must be set (validated at load, so `kind()` is infallible).",
      "type": "object",
      "properties": {
        "input": {
          "anyOf": [
            {
              "$ref": "#/definitions/InputDef"
            },
            {
              "type": "null"
            }
          ]
        },
        "pick": {
          "anyOf": [
            {
              "$ref": "#/definitions/PickDef"
            },
            {
              "type": "null"
            }
          ]
        },
        "use": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "PickDef": {
      "type": "object",
      "properties": {
        "from": {
          "description": "Shell command whose stdout lines become the options. May interpolate `{param}` for params declared *earlier* in the same command.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "options": {
          "description": "Static option list.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/definitions/OptionDef"
          }
        }
      },
      "additionalProperties": false
    },
    "PipeEntry": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "$ref": "#/definitions/PipeUseRef"
        }
      ]
    },
    "PipeGroup": {
      "type": "object",
      "required": [
        "pipe"
      ],
      "properties": {
        "pipe": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PipeEntry"
          }
        }
      },
      "additionalProperties": false
    },
    "PipeUseRef": {
      "description": "A step in a pipe contributes stdout only, so no `exports:` here.",
      "type": "object",
      "required": [
        "use"
      ],
      "properties": {
        "use": {
          "type": "string"
        },
        "with": {
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "RunEntry": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "$ref": "#/definitions/UseRef"
        },
        {
          "$ref": "#/definitions/PipeGroup"
        }
      ]
    },
    "RunSpec": {
      "description": "`run:` is a single command line (executed via `sh -c`, exactly the original behavior) or a step list compiled into one bash script (see compile.rs).",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RunEntry"
          }
        }
      ]
    },
    "StepDef": {
      "description": "A step: a plain script string, or a script with a declared contract.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "$ref": "#/definitions/FullStep"
        }
      ]
    },
    "UseRef": {
      "type": "object",
      "required": [
        "use"
      ],
      "properties": {
        "exports": {
          "description": "Rename the step's declared outputs: `{ TASK: BACKEND_TASK }`.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "use": {
          "type": "string"
        },
        "with": {
          "description": "Rebind the step's `{placeholder}` names (values may reference this command's params).",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "VarDef": {
      "type": "object",
      "properties": {
        "default": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "required": {
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    }
  }
}
